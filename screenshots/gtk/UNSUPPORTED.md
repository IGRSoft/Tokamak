# gtk — unsupported demos

Regenerated by Scripts/screenshots/gtk-docker.sh on 2026-06-19T19:18:50Z.
Each line is a real per-entry outcome from this capture run.

> NOTE: TokamakGTK4 now attaches the view tree (GTK3->GTK4 attach fix:
> gtk_window_set_child for the window child, real GtkBox for stacks), so many
> demos render REAL distinct content -> screenshots/gtk/<Name>.png. The entries
> below are the demos that still do NOT render distinctly: deny-set primitives the
> backend never implemented, and views that map a window but paint no content (the
> capture is the ~3-colour blank window chrome; rejected by the colour-count gate).
> Those need further per-primitive renderer work, which is beyond this change.

- Containers/ForEach — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Containers/Form & GroupBox — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Containers/List — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Containers/Sidebar — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Containers/OutlineGroup — deny-set: primitive not implemented in TokamakGTK4
- Containers/DisclosureGroup — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Drawing/Canvas — deny-set: primitive not implemented in TokamakGTK4
- Drawing/Color — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Drawing/Path — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Layout/LazyVStack/LazyHStack — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Layout/Grid — deny-set: primitive not implemented in TokamakGTK4
- Layout/GeometryReader — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Modifiers/Shadow — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Gestures/Gesture & CoordinateSpace — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Selectors/ColorPicker — deny-set: primitive not implemented in TokamakGTK4
- Selectors/DatePicker — deny-set: primitive not implemented in TokamakGTK4
- Selectors/Slider — deny-set: primitive not implemented in TokamakGTK4
- Selectors/Stepper — deny-set: primitive not implemented in TokamakGTK4
- Selectors/Toggle — deny-set: primitive not implemented in TokamakGTK4
- Text/Label — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Misc/Transitions — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
- Misc/ProgressView — deny-set: primitive not implemented in TokamakGTK4
- Misc/Gauge — deny-set: primitive not implemented in TokamakGTK4
- Misc/Environment — runtime: aborted before window map
- Misc/AppStorage — runtime: aborted before window map
- Containers/ScrollViewReader — runtime: window mapped but primitive painted no distinct content (2 colours; renderer incomplete for this view)
